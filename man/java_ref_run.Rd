% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/java_bridge.R
\name{java_ref_run}
\alias{java_ref_run}
\title{Run the Reference Java Maxent Pipeline}
\usage{
java_ref_run(
  bio1_vec,
  bio2_vec,
  sample_indices,
  beta_multiplier = 1,
  max_iter = 500L,
  convergence = 1e-05
)
}
\arguments{
\item{bio1_vec}{Numeric vector of raw bio1 values (length n).}

\item{bio2_vec}{Numeric vector of raw bio2 values (length n).}

\item{sample_indices}{Integer vector of 0-based background indices of
occurrence points.}

\item{beta_multiplier}{Regularization multiplier (default 1.0).}

\item{max_iter}{Maximum optimizer iterations (default 500).}

\item{convergence}{Convergence threshold (default 1e-5).}
}
\value{
Named list with fields \code{handle} (the \code{jobjRef}),
  \code{loss}, \code{entropy}, \code{gain}, \code{iterations},
  \code{num_features}, \code{num_points}, \code{lambdas}.
}
\description{
Instantiates a \code{density.MaxentRefRunner}, which wires two linear
features (\code{bio1_vec}, \code{bio2_vec}, pre-scaled internally) and
the supplied sample indices through the real \code{density.FeaturedSpace}
and \code{density.Sequential} optimizer.  Returns the underlying Java
handle together with the final scalar diagnostics.
}
