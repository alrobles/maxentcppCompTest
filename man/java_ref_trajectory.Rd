% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/java_bridge.R
\name{java_ref_trajectory}
\alias{java_ref_trajectory}
\title{Reference Per-Iteration Trajectory (real Java Sequential)}
\usage{
java_ref_trajectory(
  bio1_vec,
  bio2_vec,
  sample_indices,
  beta_multiplier = 1,
  checkpoints = c(1L, 2L, 3L, 5L, 10L, 20L, 50L, 100L, 200L, 500L)
)
}
\arguments{
\item{bio1_vec}{Numeric vector of raw bio1 values (length n).}

\item{bio2_vec}{Numeric vector of raw bio2 values (length n).}

\item{sample_indices}{Integer vector of 0-based occurrence indices.}

\item{beta_multiplier}{Regularization multiplier (default 1.0).}

\item{checkpoints}{Integer vector of 1-based iteration numbers.}
}
\value{
Data frame with columns \code{iteration, loss, entropy,
  lambda_0, lambda_1} in the order requested.
}
\description{
Runs \code{density.Sequential} via \code{density.MaxentRefRunner} and
snapshots \code{(loss, entropy, lambda_0, ..., lambda_{k-1})} at each
requested iteration number.  Convergence-based early termination is
suppressed so the trajectory always extends to \code{max(checkpoints)}.
}
\details{
Phase B of the maxentcpp fidelity plan (issues #36 / #37) uses this
helper together with the C++ trajectory binary
(\code{inst/cpp/trajectory_cpp.cpp}) and the MaxentMini
\code{trainNIterations} path to compute per-iteration
\code{||\\lambda_cpp - \\lambda_java||_\\infty} and friends on the
mock fixture.
}
